{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range .Site.Pages }}
    {{- if partial "seo/is-indexable.html" . }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
  </url>
    {{- end }}
  {{- end }}
  {{- /* Reading materials are static HTML files, not Hugo pages; list them from the data file. */}}
  {{- $reading := index .Site.Data "reading-materials" }}
  {{- range $reading.materials }}
  <url>
    <loc>{{ .reading_url | absURL }}</loc>
  </url>
  {{- end }}
</urlset>
